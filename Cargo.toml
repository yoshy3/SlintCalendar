[package]
name = "slint-calendar"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = { version = "1.6", features = ["std"] }
chrono = "0.4"

[build-dependencies]
slint-build = "1.6"

[profile.release]
opt-level = 3
